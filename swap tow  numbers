#include<stdio.h>
int main()
{
int x,y,tem;
printf("enter an value of x and y");
scanf("%d%d",&x,&y);
printf("before swappint x = %d y = %d",x,y);
x = y;
y = temp;
printf("after swapping x = %d y = %d,x,y);
return 0;
}
